//
//  AIAdventureView.swift
//  StoryVaultAI
//
//  Created by DM on 3/1/25.
//


//
//  AIAdventureView.swift
//  StoryVaultAI
//

import SwiftUI

struct AIAdventureView: View {
    var body: some View {
        VStack {
            Text("AI Adventure (Beta)")
                .font(.title)
                .padding()
            Text("Placeholder content for AI adventure.")
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Color.gray.edgesIgnoringSafeArea(.all))
    }
}

struct AIAdventureView_Previews: PreviewProvider {
    static var previews: some View {
        AIAdventureView()
    }
}
